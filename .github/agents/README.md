# Agents metadata

Place agent registration metadata in `.github/agents/<name>/agent.yml`. Each file should contain `name`, `display_name`, `description`, `prompt` (path to a prompt file), and optional `snippets` list.

Example: `.github/agents/vsa/agent.yml` -> `prompts/vsa_prompt.md`.
