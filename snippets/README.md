# Snippets

Store VS Code snippet JSON files here. Naming convention: `<role>_snippets.code-snippets`.

Each file should contain a set of named snippets with `prefix`, `body`, and `description` fields. Keep keys descriptive and align snippets to the prompt roles.
